<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item android:id="@+id/search"
            android:checked="false"
            android:icon="@drawable/ic_search_24dp"
            android:title="Search"/>

        <item android:id="@+id/home"
            android:checked="false"
            android:icon="@drawable/ic_home_24dp"
            android:title="Home"/>

        <item android:id="@+id/profile"
            android:checked="false"
            android:icon="@drawable/profile_person"
            android:title="Profile"/>

        <item android:id="@+id/browse_new"
            android:checked="false"
            android:icon="@drawable/ic_movies_24dp"
            android:title="Browse New Movies"/>

        <item android:id="@+id/recommendations"
            android:checked="false"
            android:icon="@drawable/ic_recommendation_24dp"
            android:title="Recommendations"/>

        <item android:id="@+id/signout"
            android:checked="false"
            android:icon="@drawable/ic_logout_24dp"
            android:title="Sign out"/>
        <item android:id="@+id/search"
            android:checked="false"
            android:icon="@drawable/ic_search_24dp"
            android:title="Search"/>

        <item android:id="@+id/signout"
            android:checked="false"
            android:icon="@drawable/ic_logout_24dp"
            android:title="Sign out"/>

    </group>
</menu>