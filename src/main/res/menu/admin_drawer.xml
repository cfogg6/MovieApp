<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">
        <item android:id="@+id/users"
            android:checked="false"
            android:icon="@drawable/profile_person"
            android:title="Users"/>

        <item android:id="@+id/flag_com"
            android:checked="false"
            android:icon="@drawable/ic_flag_24dp"
            android:title="Flagged Comments"/>

        <item android:id="@+id/signout"
            android:checked="false"
            android:icon="@drawable/ic_logout_24dp"
            android:title="Sign out"/>
    </group>
</menu>